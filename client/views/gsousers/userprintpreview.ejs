<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>

  <title>GsoReports</title>
</head>
<body class="p-10 h-screen w-screen">
    <h2 class="text-2xl"> <span class="font-bold"> Date:</span>  <span class="font-semibold"><%= new Date().getMonth() + 1 + '/' + new Date().getDate() + '/' + new Date().getFullYear()  %></span></h2>
  <h1 class="w-full text-center font-bold text-3xl mb-5"><span class="text-[red]">GSO</span> Inventory System Reports</h1>
  <h1 class="w-full text-center font-bold text-3xl mb-5"><%= currentUser.firstname.charAt(0).toUpperCase()%><%= currentUser.firstname.slice(1)%> Receive Reports</h1>
  <table class="w-full text-center border-collapse border">
    <thead class="">
      <tr class="border-2 py-1 border-black">
        <th class="w-40 border-r-2">Tracking No</th>
        <th class="w-48 border-r-2">Item Name</th>
        <th class="w-48 border-r-2">Item Code</th>
        <th class="w-28 border-r-2">Quantity</th>
        <th class="w-48 border-r-2">Receive Date (mm/dd/yyyy)</th>
      </tr>
    </thead>
    <tbody>
      <% for(let requestQuery of requestsQuery) { %>
      <tr class="even:bg-slate-100">
        <td class="font-mono px-2 border-r"><%= requestQuery.tracking %></td>
        <td class="border-r"><%= requestQuery.item.name %></td>
        <td class="border-r"><%= requestQuery.item.code %></td>
        <td class="border-r"><%= requestQuery.qty %></td>
        <td class="border-r"><%=requestQuery.receivedate.getMonth() + 1 + '/' + requestQuery.receivedate.getDate() + '/' + requestQuery.receivedate.getFullYear()%></td>
      </tr>
      <% } %>
    </tbody>
  </table>
   
      
      <img src="/image/icons8-print-50.png" alt="print" class=" p-3 w-14 fixed bottom-5 right-5 rounded-full shadow-[0px_0px_10px_0px_rgba(0,0,0,0.2)] bg-blue-500 text-white hover:scale-125 transition-ease-in duration-300 print:hidden" id="printImage">
      <script src="/js/printButton.js"></script>
</body>
</html>