<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>GSO Print Screen</title>
</head>
<body class="h-screen w-screen flex flex-col px-10 print:px-0 py-10">
  <h2 class="text-2xl"> <span class="font-bold"> Date:</span>  <span class="font-semibold"><%= new Date().getMonth() + 1 + '/' + new Date().getDate() + '/' + new Date().getFullYear()  %></span></h2>

  <h1 class="w-full text-center text-3xl font-bold py-2">GSO Complete Transaction </h1>
  <table>
    <thead>
      <tr class=" h-10 text-xl">
        <th class="border-2 border-black px-1">Tracking No.</th>
        <th class="border-2 border-black px-1">Item Name</th>
        <th class="border-2 border-black px-1">Department</th>
        <th class="border-2 border-black px-1">Username</th>
        <th class="border-2 border-black px-1">Quantity</th>
        <th class="border-2 border-black px-1">Receive Date</th>

      </tr>
    </thead>
    <tbody class="">
      <% for(let request of requestData) { %>
        <tr class="text-center h-10 font-semibold ">
          <td class="border-2 border-black font-mono"><%= request.tracking %></td>
          <td class="border-2 border-black"><%= request.item.name %></td>
          <td class="border-2 border-black"><%= request.department.department%></td>
          <td class="border-2 border-black"><%= request.user.username %></td>
          <td class="border-2 border-black"><%= request.qty %></td>
          <td class="border-2 border-black"><%= new Date(request.receivedate).getMonth() + 1 + '/' + new Date(request.receivedate).getDate() + '/' + new Date(request.receivedate).getFullYear()   %></td>
        </tr>
          
      <% } %>
    </tbody>
  </table>

  <img src="/image/icons8-print-50.png" alt="print" class=" p-3 w-14 fixed bottom-5 right-5 rounded-full shadow-[0px_0px_10px_0px_rgba(0,0,0,0.2)] bg-blue-500 text-white hover:scale-125 transition-ease-in duration-300 print:hidden" id="printImage">
  <script src="/js/printButton.js"></script>
</body>
</html>