<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Document</title>
</head>
<body class="h-screen w-screen flex flex-col p-10">
  <h2 class="text-2xl"> <span class="font-bold"> Date:</span>  <span class="font-semibold"><%= new Date().getMonth() + 1 + '/' + new Date().getDate() + '/' + new Date().getFullYear()  %></span></h2>

  <h1 class="w-full text-center text-3xl font-bold py-2">GSO Deployed Units </h1>
  <table>
    <thead>
      <tr class=" h-10 text-xl">
        <th class="border-2 border-black">Item Name</th>
        <th class="border-2 border-black">Item Code</th>
        <th class="border-2 border-black">Department</th>
        <th class="border-2 border-black">Quantity</th>
      </tr>
    </thead>
    <tbody class="">
      <% for(let deployed of deployedData) { %>
        <tr class="text-center h-10 font-semibold ">
          <td class="border-2 border-black"><%= deployed.item.name %></td>
          <td class="border-2 border-black"><%= deployed.item.code %></td>
          <td class="border-2 border-black"><%= deployed.department.department%></td>
          <td class="border-2 border-black"><%= deployed.qty %></td>
        </tr>
          
      <% } %>
    </tbody>
  </table>

  <img src="/image/icons8-print-50.png" alt="print" class=" p-3 w-14 fixed bottom-5 right-5 rounded-full shadow-[0px_0px_10px_0px_rgba(0,0,0,0.2)] bg-blue-500 text-white hover:scale-125 transition-ease-in duration-300 print:hidden" id="printImage">
  <script src="/js/printButton.js"></script>
</body>
</html>